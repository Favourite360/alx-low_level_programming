#include "main.h"

/**
 * _islower - will check for lower case character
 * @c : character to check the case
 * Return:0 or 1 if c lowercase
 */

int _islower(int c)
{
	return (c >= 97 && c <= 122);
}
